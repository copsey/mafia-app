#ifndef MAFIA_UTIL_MISC_H
#define MAFIA_UTIL_MISC_H

#include <utility>

#include <gsl/gsl>

namespace maf {
	using std::as_const;
	using std::declval;
	using std::move;
	using std::swap;
	using std::pair;
	using std::tuple;

	using gsl::index;
}

#endif
